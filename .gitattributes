# 这个仓库里的东西全部面向 Linux：打包脚本、shell 钩子、Makefile、GitHub Actions。
# 哪怕混进一个 CR，卸载钩子就会变成 "bad interpreter: /bin/sh^M"，
# 打出来的包在 dpkg/rpm/pacman 那里直接失败。
# 所以：所有文本一律 LF，并且写死在这里，不依赖每个人本地的 core.autocrlf。
* text=auto eol=lf

# 下面这些是要被 dpkg / rpm / pacman 直接执行或解析的，单独再钉一遍
src/bin/*           text eol=lf
src/maintainer/*    text eol=lf
src/etc/*           text eol=lf
src/data/*          text eol=lf
src/doc/*           text eol=lf
tools/*             text eol=lf
tests/*             text eol=lf
*.py                text eol=lf
*.sh                text eol=lf
Makefile            text eol=lf
*.srt               text eol=lf
*.tsv               text eol=lf
